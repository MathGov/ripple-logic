# MathGov / RippleLogic Core Release 2026.06 - Final Locked

This package is the MathGov / RippleLogic Core Release 2026.06 Root Reality Doctrine Patch, prepared as the final locked GitHub-ready release bundle.

This is a genuine-OOXML release set. All Office documents are valid Word/Excel containers, and the package is governed by the manifest hashes in `SHA256SUMS.txt`.

## Exact component pins

- RippleLogic Canon: v10.7.4
- SGP: v5.4.4
- ripple.md Standard: v3.5.4
- RippleLogic Agent System: v10.7.4
- Foundations Primer: v2.5.4
- RippleLogic Aligners Sheet: v3.5.5

Generic references such as v10.7, v5.4, or v3.5 are release-line shorthand only. Exact package identity is governed by the component pins above and the SHA-256 manifest in `SHA256SUMS.txt`.

## Reality-contact doctrine

MathGov defines reality, for decision-making, as the evaluator-independent field of constrained structures that, under transition, produce consequences. A decision never captures the whole territory; it captures a reality surface. Evidence is traceable contact with that surface. Claims must remain inside the boundary of that contact. Where the surface is insufficient for a material claim, the system must narrow, escalate, handle conservatively, or refuse.

Public teaching line: Reality is the territory. Structure is its arrangement. Constraint is its law. Consequence is its answer. Evidence is our contact. Ethics is how we act without pretending the surface is the whole.

## Core files

- `RippleLogic_v10.7.4_Canon.docx`
- `SGP_v5.4.4.docx`
- `ripple_md_Standard_v3.5.4.docx`
- `ripple_md_Standard_v3.5.4.md` - Markdown companion for GitHub reading; the DOCX remains the formatting-controlling source where table conversion differs
- `RippleLogic_Agent_System_v10.7.4.docx`
- `RippleLogic_Foundations_Primer_v2.5.4.docx`
- `RippleLogic_Aligners_Sheet_v3.5.5.xlsx`

## Claim boundary

This release is architecture-complete within its declared Tier 1-3 scope and includes a worked-run demonstration. It is not empirical validation, ProofPack readiness, Tier 4 readiness, legal certification, deployment certification, completed biological SGP measurement, current-AI sentience, solved AI alignment, or deterministic framework selection for non-decisive runs.

## Final Canon/SGP precision hardening

This final pass preserves the v10.7.4 / v5.4.4 / v3.5.4 / v10.7.4 / v2.5.4 / v3.5.5 release line. It clarifies SGP Pillar C, **Union Participation**, as responsibility-bearing participation in a shared consequence field; states that MPS/GPR/SPR are future-major-release or disclosure surfaces, not replacements for the current scalar interface; standardizes SPR as **Stewardship/Power Readiness**; and hardens the Canon-side SGP interface so C(E) cannot be used to reduce `SG_patient_norm(E)`, weaken human plateau protection, infer governance authority, or override SGP scalar/status outputs.

## Workbook sanity status

The Aligners Sheet internal sanity surface is clean in this final locked bundle: `FAIL_COUNT = 0`, `OVERALL = ✓ ALL PASS`, and SC-83 passes with the scoped label `Worked-run claimable`. The workbook remains a worked-run companion and training/audit artifact, not a Tier 4 ProofPack or empirical validator.

## Verification

From inside the extracted release folder, run:

```bash
python VERIFY_RELEASE.py
```

The verifier checks:

- SHA-256 manifest integrity (`SHA256SUMS.txt`) for every pinned file;
- OOXML container integrity, including magic bytes and required parts, for every `.docx` and `.xlsx`;
- absence of common workbook formula-error tokens;
- presence of key Canon/SGP precision-hardening integrity tokens;
- package-wide absence of the superseded SPR wording, now standardized as **Stewardship/Power Readiness**;
- presence of the required **Stewardship/Power Readiness** token in SGP, Agent System, and Aligners workbook surfaces;
- Aligners workbook internal sanity status: `FAIL_COUNT = 0`, `OVERALL = ✓ ALL PASS`, and SC-83 = `PASS`.

A clean run prints `PASS` lines and exits 0.

## License note

The release preserves per-artifact license notes where present. The ripple.md standard declares CC BY 4.0 for specification text and Apache-2.0 for tooling code. Before public repository publication, keep repository-level license files aligned with the intended licensing policy for all artifacts.

## Office note

Office files may open read-only if viewed inside the ZIP archive. Extract the release folder before editing or running workbook recalculation.

# Markdown Conversion Report

Generated from the final DOCX artifacts using Pandoc GitHub-Flavored Markdown (`gfm`) with ATX headings and no forced line wrapping.

For this release, Markdown files are GitHub-readable derivatives. The manifest-pinned DOCX controls if rendering differs.

## Symbol scan

See `RELEASE_INTEGRITY/MARKDOWN_SYMBOL_SCAN.json`.

## Known boundary

Very large DOCX tables may be less readable in GitHub Markdown than in DOCX. Future releases should consider CSV/HTML table companions for the largest canonical tables.

# v12.6 Safety and Conformance Vectors

The release includes three positive and thirteen negative MathGov run vectors, WDBIP positive and adversarial vectors, and SGP RMCP positive/negative vectors. Negative vectors are expected to fail. Their rejection demonstrates bounded machine-interface behavior, not evidence truth, empirical validity, physical safety, lawful authority, or moral truth.

Load-bearing checks include: prior-stage short circuit; no ranking of non-selectable options; no decisive-state contradiction; emergency-state serialization; execution authority; schema additional-property rejection; unknown-parameter exclusion from arithmetic; unified severe-rights activation; and required RMCP misuse fields; carried-obligation integrity for CSV controls; rejection of execution under expired material obligations; and rejection of unresolved hidden-human-compensation dependencies.


## v12.6 parameter, action-binding, and uncertainty-closure vectors

### Vector: rights saturation sensitivity

A rights-floor aggregate whose pass/fail state changes across the preregistered `beta_RF` sensitivity range MUST emit `RIGHTS_SATURATION_SENSITIVE` and MUST NOT support an unqualified RF/NCRC result until the parameter basis is governed. Example: `X_RF=-0.5`, `theta_BODY=-0.70`; `beta_RF=1.5` yields approximately `-0.635` while `beta_RF=2.0` yields approximately `-0.762`.

### Vector: phantom uncertainty cannot be zero

For a required-active empty cell, the phantom point estimate uses `c_point=1.0`, but Method B uses `c_uncertainty=0.0`; therefore `sigma=A_cell`, not zero. Any record with `UNKNOWN_IMPACT`, a phantom-active cell, and `sigma=0` is non-conformant.

### Vector: HOI interpretation

`HOI>0` with positive standardized coherence change is `RELATIVE_COHERENCE_LAG`, not structural deterioration. The strict hollowing description requires positive standardized welfare change and negative standardized coherence change under the declared EMA window.

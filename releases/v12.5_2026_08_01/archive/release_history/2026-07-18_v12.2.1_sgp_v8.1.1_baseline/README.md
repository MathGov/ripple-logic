# MathGov v12.2.1 / SGP v8.1.1 active-source baseline

This folder preserves the immediately preceding active Markdown sources and machine interface before the v12.3 integration. Binary DOCX/PDF/XLSX lineage remains available through the prior public release package.
